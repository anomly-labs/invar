#!/usr/bin/env bash
# A backend that emits the same information in a DIFFERENT line order, to prove the CLI
# selects by prefix rather than by index.
echo "coverage — something about budgets that must not be mistaken for a result"
echo "units — 42000 challenged matmul rows re-executed bit-exactly (fake) over 25 evaluations"
echo "nonce deadbeef rows/eval 64 evals 25 — 1600 challenged lm_head rows re-executed bit-exactly (fake)"
echo "ACCEPT"
exit 0
